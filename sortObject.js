let sortObject = {
    name: function(a, b) {
        

        if (a < b) {
            return -1;
        } else {
            return a > b ? 1 : 0;
        }
    },
    duration: function () {
        console.log('sorting function not yet defined');
    },
    date: function() {
        console.log('sorting function not yet defined');
    }
}